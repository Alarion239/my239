# Base URL the web client prepends to every API path. Defaults to "/api/v1"
# (relative) so requests are same-origin and get proxied to the backend by
# Vite in dev and nginx in prod. Override only for unusual setups.
VITE_API_BASE=/api/v1
