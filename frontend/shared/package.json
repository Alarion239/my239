{
  "name": "@my239/shared",
  "private": true,
  "version": "0.0.1",
  "description": "Pure-TypeScript domain layer shared by the web and mobile clients: API clients, types, validation, formatting and i18n helpers. No DOM, no React Native imports.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
