[build]
builder = "RAILPACK"
buildCommand = "go build -o migrate ./cmd/migrate && go build -o telegram-bot ./cmd/telegram-bot"

[deploy]
preDeployCommand = "./migrate up"
startCommand = "./telegram-bot"